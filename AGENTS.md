# AGENTS.md

本文件定义人工开发者和自动化编码代理在本仓库中的工作边界。除非任务另有明确说明，本文件适用于整个仓库。

## 1. 项目目标

本仓库包含现有 SunShineNewStore 电商原型，并计划新增独立的 Sunshine Inventory Management System 外挂进销存系统。当前优先级是先建立可靠的多门店批次库存基础，而不是继续扩张电商功能范围：

1. 多门店、多仓库、商品、条码和到期日期设计；
2. 库存流水、库存汇总、权限和审计；
3. 扫码入库、手工出库、调拨、盘点和临期提醒；
4. 自动测试、部署和运维基线；
5. 电商、POS、物流、支付和 AI 在库存 MVP 稳定后整合。

## 2. 仓库结构

- `backend/`：Express API、控制器、中间件及数据库脚本。
- `frontend/newSunShineFrontend/`：uni-app 微信小程序。
- `frontend/admin-vue/`：Vue 3 管理后台，作为管理端的主实现。
- `frontend/admin/`：旧版静态管理后台，仅维护必要兼容问题，不新增业务功能。
- `inventory-system/`：规划中的 NestJS + Vue 3 外挂进销存系统；创建后遵循其目录内更具体的规范。
- `docs/`：项目规范、联调文档和记录模板。

## 3. 修改原则

- 保留现有业务行为，避免与任务无关的大规模重写。
- 修改前先阅读相关路由、控制器、前端调用和数据库结构。
- 一个变更只解决一个明确问题；安全修复可同时补充必要测试和文档。
- 不把模拟数据、硬编码凭据或开发后门带入生产路径。
- 不直接编辑 `node_modules/`、`unpackage/`、`dist/` 等依赖或生成目录。
- 不提交 `.env`、令牌、密码、微信密钥、数据库备份或用户隐私数据。
- 不在没有迁移和回滚方案时破坏性修改数据库结构。
- 删除数据、重写 Git 历史、强制推送或执行生产部署前必须获得明确授权。

## 4. 后端规范

- API 路径保持 `/api/<resource>` 风格；固定路径必须定义在 `/:id` 之前。
- 受保护接口必须显式使用 `authenticateUser`；管理接口同时使用 `requireAdmin`。
- 公共注册不得接受管理员角色。
- 参数验证放在路由层，业务校验放在控制器或服务层。
- SQL 必须参数化，不拼接用户输入。
- 涉及订单和库存的事务必须使用同一个数据库连接，并保证异常回滚。
- 金额不得依赖不受控的浮点累计；新增逻辑应使用可靠的金额计算策略。
- 不向客户端返回堆栈、SQL、密钥或原始内部错误。
- 新增接口应使用统一响应结构，并为失败场景补充测试。

推荐响应结构：

```json
{
  "code": 200,
  "message": "操作成功",
  "data": {}
}
```

## 5. 前端规范

- Vue 管理端新增业务必须连接真实 API，不以 `localStorage` 充当业务数据库。
- API 基础地址从环境配置读取，不在页面中写死。
- token 存储键和 401 处理必须统一。
- 用户输入和服务端内容不得未经转义写入 `innerHTML`。
- 页面至少处理加载、空数据、失败和无权限四种状态。
- 小程序路由必须先在 `pages.json` 注册，并按 tabBar/普通页面选择正确跳转方法。

## 6. 数据库规范

- 生产结构变更使用版本化 migration；初始化样例数据与生产数据分离。
- 表和字段使用 `snake_case`，时间字段优先包含 `created_at`、`updated_at`。
- 订单、支付、库存和审计数据优先软删除或保留历史，不直接硬删除。
- 唯一性和关键完整性规则应由数据库约束保证，不能只依赖应用层检查。
- migration 必须描述升级、兼容性和回滚方式。

## 7. 测试和检查

提交前按变更范围执行：

- 后端：语法检查、认证/权限、参数校验及相关 API 集成测试。
- 管理端：构建检查及受影响页面测试。
- 小程序：目标平台编译和核心路径冒烟测试。
- 数据库：在非生产数据库验证 migration、事务和回滚。
- 文档：检查链接、命令、环境变量名称和当前实现是否一致。

如果受环境限制无法执行某项检查，必须在交付说明中写明原因，不能声称已经通过。

## 8. Git 和存档规范

- 分支建议：`codex/<topic>`、`feat/<topic>`、`fix/<topic>`、`docs/<topic>`。
- 提交信息建议遵循 Conventional Commits，例如 `fix(auth): prevent public admin registration`。
- 不提交依赖目录、构建产物、日志、覆盖率、环境文件和本地 IDE 配置。
- 功能、修复、部署和测试结果分别记录到 `CHANGELOG.md` 或 `docs/records/`。
- 使用 `docs/templates/` 中的模板创建开发日志、测试报告、部署记录和每周总结。

## 9. 完成定义

一项工作只有在以下条件满足时才算完成：

- 需求和验收条件已满足；
- 没有引入已知越权、数据一致性或敏感信息泄漏；
- 已执行与风险相称的检查；
- 文档和变更日志已同步；
- 未完成项、限制和后续工作已明确记录。
